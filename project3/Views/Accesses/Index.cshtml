@model IEnumerable<project3.Models.Access>

@{
    ViewBag.Title = "Access";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Level)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateIDK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DoubleIDK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LongIDK)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Level)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateIDK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DoubleIDK)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LongIDK)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
